spring:
  application:
    name: schatServer

springdoc:
  api-docs:
    enabled: true #swagger ?? ?? ??
  swagger-ui:
    path: /index.html #swagger path
    #groups-order: DESC
    doc-expansion: list #??? ??? ?? ?? (none: ??, full: ????)
    tags-sorter: alpha # alpha(??? ????), method(HTTP????)
    operationsSorter: method #?? ? API ?? ??
    #disable-swagger-default-url: true
    #display-request-duration: true
